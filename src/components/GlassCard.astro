---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = '', style = '' } = Astro.props;
---

<div class={`glass-card rounded-xl ${className}`} style={style}>
  <slot />
</div>
